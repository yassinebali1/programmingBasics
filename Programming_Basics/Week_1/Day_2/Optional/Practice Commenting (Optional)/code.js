// This variable stores the current count of likes, initialized to 0.
var likeCount = 0;
// This function increases the like count by 1 whenever it is called.
function increaseLikes() {
    // This increments the like count by 1.
    likeCount = likeCount + 1;
}
